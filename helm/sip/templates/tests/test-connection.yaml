apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sip.fullname" . }}-test-connection"
  labels:
    {{- include "sip.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sip.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
